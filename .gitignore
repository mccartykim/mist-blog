*.beam
*.ez
/build
erl_crash.dump

# Personal content and assets
priv/content/blog/
priv/assets/

# Per-developer / per-agent local overrides (CLAUDE.local.md, .envrc.local,
# etc.) — anything with a .local. segment in its filename stays out of VCS.
*.local.*

# Sensitive files
server.log
tags_index.png

# Build artifacts and caches
*.log
*.tmp
.cache/

# Nix development
result*
